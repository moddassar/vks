apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-argo
  name: nginxsvc-argo
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-argo
  type: NodePort
